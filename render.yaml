services:
  - type: web
    name: nexa-tools-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: SERPER_API_KEY
        sync: false
      - key: NODE_VERSION
        value: 22.22.2